# StudentAid v0.19.0 Step24 源码正式版

版本：`v0.19.0-step24-gui-realtime-metrics`  
标签：`stable-2026-08-12-step24-gui-realtime-metrics`  
日期：2026-08-12  
平台：Windows 10/11 x64

## 本版更新

- 正式源码升级为 `ait19.py`，保留 `ait18.py` 和 `ait17.py` 供回溯。
- GUI 新增实时效率看板：完成百分比、已处理/剩余/待处理/处理中数量、最近一分钟处理数量、批次平均速度、预计剩余时间和已运行时间。
- 速度以首个数据库进度快照为基线，导入阶段已经确定的格式错误不计入浏览器吞吐量；并发旧快照不会让进度倒退。
- 包含 Step23 的结果持久化流水线优化：浏览器 worker 不再等待 XLSX 整表逐条重写，累计 CSV 实时落盘后即可继续下一条。
- 网页识别、Continue、Cancel、每线程独立浏览器复用、输入删除、累计输出、动态列和 DOB 逻辑保持不变。

## GUI 实时字段

- 进度百分比与已处理/总数
- 剩余、待处理、处理中
- 最近滚动 60 秒处理数量
- 批次平均每分钟处理量
- 预计剩余时间和批次已运行时间
- 明确结果、失败、停止数量

## 验证

- `ait18.py` 基线：26/26 测试通过。
- `ait19.py`：30/30 测试通过。
- Tkinter GUI 控件构建、滚动速度、平均速度、ETA、乱序进度和结束冻结测试通过。
- `启动StudentAid.cmd` 安装检查通过，入口已切换为 `ait19.py`。
- 发布包不包含 EXE、测试资料、累计输出、SQLite、GUI 配置、缓存、虚拟环境或浏览器数据。

## 使用

下载 `StudentAid-Step24-Source-20260812.zip`，完整解压后双击 `启动StudentAid.cmd`。Python、Google Chrome 和依赖存在时自动跳过安装，缺少时自动安装。
